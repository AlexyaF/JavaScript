/**
 * Adição + , subtração -
 * Potenciaçõ **
 *  Resto da divisão %
 */

// Segue as regras matematicas para processar conforme a precedência da operação

// Contador ++, incrementa de 1 em 1
let contador = 2;
contador++;
contador++;
console.log(contador);

//Decremento, --
contador--;
console.log(contador);

//Atribuição ao próprio valor (Usar qualquer operador)
contador += contador; //contador + contador 
console.log(contador)