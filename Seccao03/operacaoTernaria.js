//condição ternária
//(condição) ? 'valor verdadeiro' : 'valor falso';

const pontuacaoUsuario = 100;
const nivelUsuario = pontuacaoUsuario >= 100? 'VIP': 'NORMAL';

// isso poderia ser substituido por um IF, porém é para melhor vizualização do código, manter uma condição em apenas uma linha