// && = AND 
// || = origin
// ! = NOT

console.log(true && true); // todas expressães precisam ser verdadeiras para retornar true
console.log(true || false); // apenas uma precisa ser verdadeira para retornar true - sempre retorna o primeiro valor verdadeiro

console.log(!true); // inverte o valor/nega o valor

// valores que são conaiderados FALSY (não é o falso literal mas rtorna FALSE):
// 0, '' "" ``, null, undefined, NaN